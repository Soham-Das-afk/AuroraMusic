AuroraMusic v3.2.37
====================

Release Date: 2025-10-14

Summary
-------
Documentation / metadata bump to refresh latest release with explicit notes. No functional code logic changed beyond version metadata.

Highlights
----------
- Keeps multi-arch Docker publishing (linux/amd64 + linux/arm64) introduced earlier.
- Retains automated curated release body generation (custom changelog + recent commits + image manifest info).
- Ensures users see updated version in `/ping` and README.

Changes
-------
### Changed
- Version bumped to 3.2.37 (settings, admin fallback, README).

### Unchanged (Still Available)
- Automated cache management, banner/thumbnail toggles, daily auto-restart scheduler.
- Audio caching + previous track reuse.
- Structured logging throughout.
- Release workflow: multi-arch build + manifest inspection + custom release body assembly.

Upgrade Impact
--------------
Purely informational update. You only need to pull if you want the version string to reflect 3.2.37.

Upgrade Commands
----------------
Docker (Compose):
```
docker compose pull
docker compose up -d --pull always
```
Direct Docker:
```
docker pull sohamdas103/auroramusic:3.2.37
```
Source:
```
git pull
python src/bot.py
```

Roadmap (Next Candidates)
-------------------------
- Optional vulnerability scan (Trivy) in CI.
- /health expansion: show disk cache size & next restart ETA.
- CodeQL / security static analysis.
- Dynamic badges once repository becomes public.
